NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
# Must end with /chat (namespace). If omitted, /chat is appended automatically.
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000/chat
# Public site URL (HTTPS in production — required for location, camera, microphone)
NEXT_PUBLIC_APP_URL=https://link-chats.com